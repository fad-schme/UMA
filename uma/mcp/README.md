# mcp/ — Moved

The MCP server has moved into the `uma` package as of v0.2.0.

- **Entry point:** `uma-mcp` (console script — install with `pip install 'uma-mem[mcp]'`).
- **Source:** [`server.py`](server.py).
- **Client configuration:** [`docs/mcp/STDIO_CLIENTS.md`](../../docs/mcp/STDIO_CLIENTS.md).

This folder will be removed in v0.3.0.
